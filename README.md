# Node Express Project

This is a basic Node Express project scaffold.

## Getting Started

1. Clone the repository
2. Run `npm install` to install dependencies
3. Run `npm start` to start the server

## API Endpoints

* GET /api: Welcome to the API